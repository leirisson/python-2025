# Passos para infra da api com FastAPI 

``criar ambiente virtual:``
python -m venv .venv

``instalção:``
pip install "fastapi[standard]"

``salvar as dependencias no arquivo requirements:``
pip freeze > requirements.txt
